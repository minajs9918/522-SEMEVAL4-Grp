words <- myCorpus %>%
filter(is_retweet == FALSE) %>%
mutate(text = str_replace_all(text, replace_reg, "")) %>%
unnest_tokens(word, text, token = "tweets")
writeLines(as.character(myCorpus))
View(Textprocessing)
load("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining/groundedemotionTextMining/.RData")
q()
setwd("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining")
library(readr)
grounded <- read_csv("grounded.csv", col_names = FALSE)
View(grounded)
grounded %>% select(X3)
View(grounded)
write.table(grounded, file = “my_data.txt”, cols_only(X3), sep = "\t",
row.names = FALSE)
write.table(grounded, file = “my_data.txt”, col.names=X3 , sep = "\t")
library(dplyr)
grounded %>%
select(X3)
data <- dplyr::select ( df,X3)
write.table(grounded, file = “my_data.txt”, sep = “\t”, rownames = FALSE)
data(grounded)
write.table(grounded.csv, file = “my_data.txt”, sep = “\t”, rownames = FALSE)
colnames(grounded)
data -> grounded$X3
select(X3)
write.table(myobject[,c("X3")], file="myCorpus.txt",row.names=FALSE)
write.table(grounded[,c("X3")], file="myCorpus.txt",row.names=FALSE)
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
removeURL <- function(x) gsub("http[^[:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeURL)
)
# remove anything other than English letters or space
removeNumPunct <- function(x) gsub("[^[:alpha:][:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeNumPunct))
# remove extra whitespace
myCorpus <- tm_map(myCorpus, stripWhitespace)
# Remove numbers
myCorpus <- tm_map(myCorpus, removeNumbers)
# Remove punctuations
myCorpus <- tm_map(myCorpus, removePunctuation)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
View(removeURL)
View(Textprocessing)
View(Textprocessing)
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('#\\S+', '', x) ## Remove Hashtags
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
gsub('[[:punct:]]', '', x) ## Remove Punctuations
gsub("^[[:space:]]*","",x) ## Remove leading whitespaces
gsub("[[:space:]]*$","",x) ## Remove trailing whitespaces
gsub(' +',' ',x) ## Remove extra whitespaces
}
# remove anything other than English letters or space
removeNumPunct <- function(x) gsub("[^[:alpha:][:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeNumPunct))
# remove extra whitespace
myCorpus <- tm_map(myCorpus, stripWhitespace)
# Remove numbers
myCorpus <- tm_map(myCorpus, removeNumbers)
# Remove punctuations
myCorpus <- tm_map(myCorpus, removePunctuation)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
gsub('[[:punct:]]', '', x) ## Remove Punctuations
gsub("^[[:space:]]*","",x) ## Remove leading whitespaces
gsub("[[:space:]]*$","",x) ## Remove trailing whitespaces
gsub(' +',' ',x) ## Remove extra whitespaces
}
# remove anything other than English letters or space
removeNumPunct <- function(x) gsub("[^[:alpha:][:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeNumPunct))
# remove extra whitespace
myCorpus <- tm_map(myCorpus, stripWhitespace)
# Remove numbers
myCorpus <- tm_map(myCorpus, removeNumbers)
# Remove punctuations
myCorpus <- tm_map(myCorpus, removePunctuation)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("https://t.co/[A-Za-z\\d]+|http://[A-Za-z\\d]+|&amp;|&lt;|&gt;|RT|https",'', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
gsub('[[:punct:]]', '', x) ## Remove Punctuations
gsub("^[[:space:]]*","",x) ## Remove leading whitespaces
gsub("[[:space:]]*$","",x) ## Remove trailing whitespaces
gsub(' +',' ',x) ## Remove extra whitespaces
}
# remove anything other than English letters or space
removeNumPunct <- function(x) gsub("[^[:alpha:][:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeNumPunct))
# remove extra whitespace
myCorpus <- tm_map(myCorpus, stripWhitespace)
# Remove numbers
myCorpus <- tm_map(myCorpus, removeNumbers)
# Remove punctuations
myCorpus <- tm_map(myCorpus, removePunctuation)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('amp', '', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
gsub('[[:punct:]]', '', x) ## Remove Punctuations
gsub("^[[:space:]]*","",x) ## Remove leading whitespaces
gsub("[[:space:]]*$","",x) ## Remove trailing whitespaces
gsub(' +',' ',x) ## Remove extra whitespaces
}
# remove anything other than English letters or space
removeNumPunct <- function(x) gsub("[^[:alpha:][:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeNumPunct))
# remove extra whitespace
myCorpus <- tm_map(myCorpus, stripWhitespace)
# Remove numbers
myCorpus <- tm_map(myCorpus, removeNumbers)
# Remove punctuations
myCorpus <- tm_map(myCorpus, removePunctuation)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
q()
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
rm_twitter_url(myCorpus)
ex_twitter_url(myCorpus)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
gsub('[[:punct:]]', '', x) ## Remove Punctuations
gsub("^[[:space:]]*","",x) ## Remove leading whitespaces
gsub("[[:space:]]*$","",x) ## Remove trailing whitespaces
gsub(' +',' ',x) ## Remove extra whitespaces
}
# remove anything other than English letters or space
removeNumPunct <- function(x) gsub("[^[:alpha:][:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeNumPunct))
# remove extra whitespace
myCorpus <- tm_map(myCorpus, stripWhitespace)
# Remove numbers
myCorpus <- tm_map(myCorpus, removeNumbers)
# Remove punctuations
myCorpus <- tm_map(myCorpus, removePunctuation)
rm_twitter_url(myCorpus)
ex_twitter_url(myCorpus)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
rm_twitter_url(myCorpus)
ex_twitter_url(myCorpus)
# remove anything other than English letters or space
removeNumPunct <- function(x) gsub("[^[:alpha:][:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeNumPunct))
# remove extra whitespace
myCorpus <- tm_map(myCorpus, stripWhitespace)
# Remove numbers
myCorpus <- tm_map(myCorpus, removeNumbers)
# Remove punctuations
myCorpus <- tm_map(myCorpus, removePunctuation)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# convert to lower case
myCorpus <- tm_map(myCorpus, content_transformer(tolower))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
## Remove Twitter Short URL
rm_twitter_url(myCorpus)
ex_twitter_url(myCorpus)
# remove anything other than English letters or space
removeNumPunct <- function(x) gsub("[^[:alpha:][:space:]]*", "", x)
myCorpus <- tm_map(myCorpus, content_transformer(removeNumPunct))
# remove extra whitespace
myCorpus <- tm_map(myCorpus, stripWhitespace)
# Remove numbers
myCorpus <- tm_map(myCorpus, removeNumbers)
# Remove punctuations
myCorpus <- tm_map(myCorpus, removePunctuation)
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
Q()
q()
q()
x -> "The Chemical Brothers - Galvanize https://t.co/vUwczAbKfW via @YouTube"
strings <- c("The Chemical Brothers - Galvanize https://t.co/vUwczAbKfW via @YouTube")
gsub('http\\S+\\s*', '', strings) ## Remove URLs
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
}
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
}
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
}
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
}
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# remove URLs
function(x)
{gsub('http\\S+\\s*', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
}
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# remove URLs
function(x) gsub('http\\S+\\s*', '', x) ## Remove URLs
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# remove URLs
gsub('http\\S+\\s*', '', myCorpus) ## Remove URLs
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
# remove URLs
gsub('http\\S+\\s*', '', myCorpus[[3]]) ## Remove URLs
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
#load tm package
library(tm)
#Create Corpus from .txt
myCorpus <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/522-SEMEVAL4-Grp/522_GrpSEMEVAL_site/R/groundedemotionTextMining"))
#View Corpus Information
myCorpus
summary(myCorpus)
#inspect a particular document
writeLines(as.character(myCorpus[[3]]))
#remove ������ what would be emojis
myCorpus<-tm_map(myCorpus, content_transformer(gsub), pattern="\\W",replace=" ")
# remove URLs
function(x)
{gsub("http[[:alnum:][:punct:]]*",'', x)
gsub('http\\S+\\s\/\/t\.co\/([A-Za-z0-9]|[A-Za-z]){10}', '', x) ## Remove URLs
gsub('\\b+RT', '', x) ## Remove RT
gsub('@\\S+', '', x) ## Remove Mentions
gsub('[[:cntrl:]]', '', x) ## Remove Controls and special characters
gsub("\\d", '', x) ## Remove Controls and special characters
}
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(myCorpus[[3]]))
