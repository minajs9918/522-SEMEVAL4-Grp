---
title: "Markdown WordCloud"
author: "P-COM 0145019"
date: "11/5/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown
```{r}
#check working directory
getwd()
```
```{r}
#load tm package
library(tm)
```
```{r}
#Create Corpus from .txt
docs <-Corpus(DirSource("D:/BackUp Mr.Wai/BAckup 20.07.2018/Documents/Operadownload/SEM 3-CODE_CDS_ 522/tripadvisorSaanen/gutenbergCharles"))
```
```{r}
#View Corpus Information
docs
summary(docs)
```
```{r}
#inspect a particular document (e.g. the 1st doc)
writeLines(as.character(docs[[2]]))
```
```{r}
#checkouttmpackage transformation functions
getTransformations()
```
```{r}
#create toSpacecontent transformer
toSpace <- content_transformer(function(x,pattern) {return (gsub(pattern," ",x))})
```
```{r}
#eliminate hyphen using toSpacecontent transformer
docs <- tm_map(docs, toSpace, "-")
```
```{r}
#apply removePunctuation
docs <- tm_map(docs, removePunctuation)
```
```{r}
#convert corpus to lower case
docs <- tm_map(docs, content_transformer(tolower))
```
```{r}
#remove digits in corpus
docs <- tm_map(docs, removeNumbers)
writeLines(as.character(docs[[2]]))
```
```{r}
#remove stopwordsusing the standard list in tm
docs <- tm_map(docs, removeWords, stopwords("english"))
writeLines(as.character(docs[[2]]))
```
```{r}
#remove whitespace (optional to remove extra whitespace)
docs <- tm_map(docs, stripWhitespace)
writeLines(as.character(docs[[2]]))
```
```{r}

#load SnowballCpackage
library(SnowballC)
```
```{r}
#duplicate object for testing
docs.stem<-docs
```
```{r}
#stem the corpus
docs.stem<-tm_map(docs.stem, stemDocument)
writeLines(as.character(docs.stem[[2]]))
```
```{r}
#load textstempackage
library(textstem)
```
```{r}
#duplicate object for testing
docs.lemma <- docs
```
```{r}
#lemmatize the corpus (require textstem)
docs.lemma <- tm_map(docs.lemma, lemmatize_strings)
writeLines(as.character(docs.stem[[2]]))
```

